# My NixOS config
The structure is generally inspired by (I'd say stolen from) [librephoenix config](https://github.com/librephoenix/nixos-config), although I made it so the tree is more or less flat. Currently is still WIP.
